<a href="{{ config('adminlte.logo.url_home') }}" class="logo">

    <span class="logo-mini">{{ config('adminlte.logo.text_small') }}</span>

    <span class="logo-lg">{{ config('adminlte.logo.text') }}</span>

</a>